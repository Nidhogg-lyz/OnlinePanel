# OnlinePanel
Using java to build a canvas and provide some additional functionality

To run it, first set the _host_ip_ in Client.java to the Server's ip address, and run Server.main() on server and then run Client.main() on clients.
